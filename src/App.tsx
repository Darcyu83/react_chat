import React from "react";
import Router from "./router/Router";

interface IProps {}

function App(props: IProps) {
  return (
    <div style={{}}>
      <Router />
    </div>
  );
}

export default App;
